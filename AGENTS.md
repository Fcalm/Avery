# Avery 代码规范与执行约束

## 一、基本原则

- 独立分析需求，不盲目附和提出者的判断；发现前提、边界或方案有问题时，应明确说明风险并提出替代方案。
- 修复 Bug 必须先复现并定位根因，再修改最小必要范围；禁止用隐藏报错、重复刷新、硬编码或扩大容错掩盖问题。
- 修改前确认影响范围、数据流和既有约束；修改后验证原场景、边界场景和相关回归场景。
- 安全、数据完整性和可维护性优先于短期实现速度，性能优化不得降低安全边界。
- 保留用户已有改动，不擅自覆盖、回滚或删除无关内容。

## 二、Git 提交与分支

- 提交格式采用 Conventional Commits：`type(scope): 中文说明`。
- 常用类型：`feat`、`fix`、`refactor`、`perf`、`test`、`docs`、`build`、`ci`、`chore`、`revert`。
- 示例：`feat(chat): 新增会话级 usage 统计`；`fix(layout): 修复窄窗口下输入框畸变`。
- 每次提交只包含一个逻辑变更；提交说明必须准确描述实际内容，不使用“更新”“修改”等模糊表述。
- 新功能、大范围重构或大量文件修改必须从主分支创建独立分支，如 `feature/功能名`、`fix/问题名`、`refactor/范围名`。
- 禁止在主分支直接开发上述改动；完成构建、测试、代码审查和必要的手工验证后方可合并。
- 提交前检查 `git status` 与 `git diff`，不得提交密钥、数据库、日志、缓存、依赖目录或构建产物。

## 三、Electron 架构与安全

- 保持职责分离：渲染进程负责界面，preload 暴露最小能力，主进程负责窗口、生命周期和系统能力调度。
- 渲染进程不得直接访问 Node.js；保持 `contextIsolation: true`、`nodeIntegration: false`，不得为图方便放宽安全配置。
- IPC 接口必须最小化、命名清晰，并校验输入、权限和返回值；错误结构应稳定且可追踪。
- 主进程不得承担长时间计算或阻塞式 I/O；重任务使用 Worker、Utility Process 或独立后端处理。
- 优先使用异步 API，禁止同步 IPC、同步文件操作及其他会阻塞主进程或渲染进程的调用。

## 四、Electron 性能约束

- 优化前先测量并记录基线，使用 DevTools、Performance、Tracing 或日志定位 CPU、内存、I/O 和启动瓶颈。
- 引入依赖前评估体积、加载耗时、资源占用和维护成本；已有轻量能力可满足时不新增重型依赖。
- 重型模块与非首屏功能按需加载，昂贵初始化延迟到实际使用时执行。
- 渲染进程中的 CPU 密集任务移交 Web Worker；低优先级任务可在空闲时调度，避免阻塞交互和首屏渲染。
- 减少不必要的网络请求；稳定静态资源优先随应用打包，核心功能应尽量不依赖启动阶段网络可用性。
- 面向 Electron 自带的 Chromium 和现代 ECMAScript，避免无必要的 polyfill 与兼容层。
- 生产代码应合理打包，减少大量小模块的加载开销；不需要默认菜单时应在应用就绪前移除。
- 性能改动必须附带可复现的对比数据，并验证启动、窗口响应、内存占用及核心流程无回归。

## 五、代码与注释

- 使用清晰命名、单一职责和显式类型；避免无依据的 `any`、重复逻辑、魔法数字和隐式共享状态。
- 注释统一使用中文，重点说明“为什么”、业务约束、不变量、边界条件、副作用和失败处理。
- 公共接口、IPC、并发逻辑、数据迁移、安全校验及关键性能策略必须有完整注释。
- 注释不得逐句翻译代码或保留失效描述；修改实现时同步更新相关注释和文档。

## 六、执行与验证

- 开始工作前先阅读当前目录及上级目录适用的 `AGENTS.md`，并遵循更具体目录中的补充约束。
- 大改动先说明方案、风险和验证方法；需求存在明显问题时先提出质疑，不以实现代替判断。
- Bug 修复需记录复现条件、根因、修复点和防回归验证；条件允许时补充自动化测试。
- 完成修改后至少运行受影响范围的类型检查、测试和构建；涉及界面或打包时增加手工冒烟验证。
- 无法完成或无法验证的部分必须明确报告原因、影响和建议的后续动作，不得宣称已完成。
