# 前端 DESIGN v2 迁移进度

> 本次任务：按 `docs/DESIGN.md`（信封 · 火漆 v2.0）修改前端代码，使界面符合新设计规范与约束。
> 状态：代码修改完成，构建验证因环境缺少 `node_modules`/网络不可用未能执行；暂保留本文档，待验证通过后删除。

## 已完成

- [x] 更新 `src/styles/tokens.css`：色板、字阶、圆角、阴影、焦点环全部对齐 DESIGN v2。
- [x] 更新 `src/styles/global.css`：清理旧版非刻度圆角/间距、旧字阶、旧侧栏尺寸、抽屉/弹窗尺寸；修正按钮、焦点环、导航激活态、抽屉/弹窗圆角与宽度。
- [x] 更新组件中的图标尺寸，只使用 12/16/20/24 四档。
- [x] 更新 `src/app/AppShell.tsx` 的侧栏宽度范围与折叠宽度常量。
- [x] 更新 `index.html` 主题色。
- [ ] 运行构建与检查：环境无 `node_modules`，`npm install` 超时/离线缓存缺失，未能执行 `npm run build`；已通过静态检查（CSS 括号平衡、色值仅存在于 tokens、圆角/间距/字号刻度、图标尺寸）。
- [ ] 验证通过后删除本进度文档。

## 关键差异记录

- 旧强调色橙 `#C4530A` → 新牛皮棕 `#C9A878`，强调文字用赭棕 `#8A6B45`。
- 旧灰墨文字 → 新暖褐墨 `#6B5F4C / #978871`。
- 圆角统一为 4/8/12/16/999。
- 间距统一使用 4/8/12/16/24/32/48/64（卡片 20 仅限紧凑卡片；原生标题栏 36px 作为窗口高度保留）。
- 字阶收敛为 DESIGN 九档：11/12/13/14/16/18/24/32。
