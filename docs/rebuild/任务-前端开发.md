# 前端开发 · 重建任务

> 归属：`docs/rebuild/PROGRESS-2026-08-20.md` 下一步计划的执行分解
> 分工日期：2026-08-20
> 角色边界：React Renderer（`src/`）、前端 API 层（`src/features/*/api`、`src/shared/platform`）、样式与 DESIGN v2 收敛、前端功能回归。**不负责** Electron Main/Preload/Gateway 源码重建（见后端任务 B-04），但负责消费侧契约核对。
> 顶层约束：`AGENTS.md`（Conventional Commits、分支规范、验证要求）、`docs/DESIGN.md`（界面唯一视觉依据，硬约束违反即退回）。

## 1. 任务总览与顺序

```text
F-01 构建解阻（最先） ──→ F-02 design-v2 收敛（P1）
                            │
        F-03 契约消费侧核对（随 B-04 推进）──→ F-04 里程碑 F 回归 ──→ F-05 里程碑 G UI ──→ F-06 专项文档
```

- F-01 是**全团队 P0 前置**，完成后才能解锁后端 B-01/B-02。
- F-02、F-03、F-06 可与后端任务并行；F-04 依赖 B-04（Desktop/Preload TS 重建）完成。

## 2. 任务清单

### F-01【P0 · 最先执行】恢复 `src/features/workspace/` 前端 API 模块

- **背景**：`.gitignore` 的 `workspace/` 规则匹配任意层级目录，导致 `src/features/workspace/`（`useWorkspaceData`、`workspaceData`、`mutationHelper`、`workspaceActions`）从未入库，当前检出缺失，根构建红。
- **动作**：
  1. 核对 `origin/refactor/frontend-design-v2` 上提交 `1dc13c5 fix(workspace): 恢复被 gitignore 误忽略的工作空间前端 API` 的内容（确认是否即为该模块）。
  2. 将其内容带到当前分支（cherry-pick 或等价方式），确保 `src/features/workspace/**` 全部文件入库。
  3. 修改 `.gitignore`：增加 `!src/features/workspace/` 反例（放在能覆盖该路径的位置，不得用全局无前缀反例）。
- **验收**：`git ls-files` 含 `src/features/workspace/**`；`npm run build:renderer` 通过；`git status` 干净。
- **依赖**：无（最优先）。
- **建议窗口**：0.5 天。
- **提醒**：提交信息遵循 `fix(workspace): …` 格式；完成后立即通知后端开发跑 B-01 全量验证。

### F-02【P1】design-v2 分支收敛核对

- **背景**：`origin/refactor/frontend-design-v2` 相对 main 含 4 个提交（DESIGN v2 信笺火漆主题迁移、导航选中态、workspace API 恢复、package-lock 生成），长期未合并，与主流程脱节。
- **动作**：
  1. 前端主导评审 DESIGN v2 迁移内容：对照 `docs/DESIGN.md` 硬约束（4 主色 + 派生 token、5 档圆角、4 档图标、间距 4px 刻度、无纯黑/纯白/橙色、对比度 ≥4.5:1、`prefers-reduced-motion`）。
  2. 核对与最新 `packages/contracts`、后端重建后的兼容性（重点：`src/styles/tokens.css`、`src/shared/` 组件）。
  3. 与后端开发共同把该分支收敛合并回 main（内容拆分为：构建修复 + DESIGN v2 + lockfile，分别评审）。
- **验收**：合并后 `tokens.css` 与 DESIGN.md 一致；视觉冒烟通过；无遗留旧橙色/灰墨色 token。
- **依赖**：F-01；与 B-02（lockfile 合入）协调。
- **建议窗口**：1 天。

### F-03【P1】契约消费侧一致性核对（配合里程碑 E）

- **背景**：ARCHITECTURE 要求 `packages/contracts` 是跨进程协议唯一来源，禁止四份手工 channel 清单。
- **动作**：随 B-04（Desktop/Preload TS 重建）推进，逐项核对 `src/shared/platform/platformClient.ts` 暴露的每个方法（channel、参数、返回信封、错误码）与 Preload/Gateway/Backend Router 契约注册表一致；产出差异清单并推动对齐；为契约测试准备消费侧 fixture。
- **验收**：platformClient 与契约注册表 100% 对齐，无重复手工清单；契约测试绿。
- **依赖**：F-01；B-04 的 preload/gateway 部分完成后开始，随 B-04 增量推进。
- **建议窗口**：随 B-04 并行，约 1–2 天工作量。

### F-04【P1 · 当前自动化范围复审通过】里程碑 F · 前端功能回归

- **动作**：按 VERSION-ROADMAP 0.2.0 与 PRD 第 8 节逐项验证：
  1. 首次引导、设置、模型配置与连接测试；
  2. 会话、消息流、停止、失败重试、重载与历史恢复；
  3. 会话独立 usage 与项目环境恢复（切换会话不串数据）；
  4. 简历、岗位、投递、档案、附件、备份与导出页面；
  5. 求职助手拖拽、最小宽度、紧凑输入区、侧栏固定控件；
  6. 1280×800 与 1024×680 关键控件无遮挡、拖动不畸变，核心流程可键盘操作。
- **验收**：关键页面截图与运行包无重大回归；与后端开发共同完成 Electron 冒烟（Renderer 加载、Backend ready）；回归问题记录并指派修复。
- **依赖**：B-04 完成（应用可完整运行）、F-03 完成。
- **建议窗口**：2–3 天。

**PM 复审（2026-08-20）**：双分辨率六主页面的布局与关键控件检查通过，本轮前端交付接受。真实 Provider、跨会话隔离、键盘/焦点/拖拽及 Windows 安装升级门禁未关闭，因此 F-04 不标记为完全完成；详见 `F-04-FRONTEND-REGRESSION-2026-08-20.md` 与 `A-02-F-04-REVIEW-2026-08-20.md`。

### F-05【P2 · 代码复审通过，待真实 Provider 复核】里程碑 G · 前端真实 Usage 展示

- **动作**：按 PRD 7.10.1 实现：会话 UI 展示 Provider 返回的真实 usage（`prompt_tokens / context_limit` 百分比）；Provider 未返回时显示"未知"；新会话显示"—"而非 0%；进入会话立即恢复 usage，不等下一次请求；详情仅展示必要数值，不显示估算图表。
- **验收**：PRD 7.10.1 全部条目通过；无估算值冒充真实值（验收次数 0）。
- **依赖**：F-04；与 B-05（数据侧）、A-04（agent 侧）共同联调。
- **建议窗口**：1 天。

**PM 复审（2026-08-21）**：Renderer 消费侧与 Mock 三方对账通过；真实百分比、Provider 未返回“未知”、新会话“—”及会话切换迟到响应隔离符合 PRD。待 DeepSeek 真实门禁具备凭据后只复核实际 UI 数据。详见 `F-05-REAL-USAGE-2026-08-21.md` 与 `A-03-G-CODE-REVIEW-2026-08-21.md`。

### F-06【P2 · 已完成】前端专项文档

- **动作**：补 PRD 专项文档清单第 9 项「前端交互、响应式布局与可访问性规范」，沉淀 F-04 回归中的约定（窗口档位、紧凑输入区规则、键盘路径、焦点态）。
- **依赖**：F-04 完成后编写。
- **建议窗口**：0.5 天。

**PM 复审（2026-08-21）**：`docs/modules/frontend-interaction-responsive-accessibility.md` 通过，已覆盖窗口档位、输入区、拖拽、键盘、焦点与回归证据规则。

## 3. 提交与协作约定

- 提交格式 `type(scope): 中文说明`，每次一个逻辑变更；新功能/大改从 main 建分支，禁止在 main 直接开发。
- 不把生成物（`dist/`、`release-*/`）提交入库；提交前检查 `git status` 与 `git diff`。
- 页面不得直接访问 `window.avery*`，一律经 `shared/platform`（ARCHITECTURE 6.2）。
- 与后端开发的接口以 `packages/contracts` 为唯一来源，发现漂移先记录到 F-03 差异清单，不擅自改通道名。
