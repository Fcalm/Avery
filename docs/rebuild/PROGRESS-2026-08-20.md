# 重建进度核验与下一步计划（2026-08-20）

> 文档性质：恢复记录（对应 `docs/PROJECT-RECONSTRUCTION-PLAN.md` 里程碑 A.3「在 `docs/rebuild/` 维护恢复记录、缺失清单和验证结果」）
> 核验人：项目经理（统筹规划，不承担开发）
> 核验日期：2026-08-20
> 核验基线：分支 `feature/backend-ts-rebuild` @ e330759，工作区 `/home/fanwq/code/Offerget`（origin: `ssh://git@ssh.github.com:443/Fcalm/Offerget.git`）

## 1. 里程碑进度总览

| 里程碑 | 状态 | 依据与缺口 |
| --- | --- | --- |
| A 冻结证据与源码基线 | 🟡 基本完成 | 基线提交 `b493532` 已建立；**缺口：`docs/rebuild/` 恢复记录目录此前不存在（本文为首条记录）** |
| B 工程骨架与可重复安装 | 🟡 部分完成 | workspace/根 package.json/Vite/Electron Builder 配置在；F-01 后根构建已绿；**缺口：无 `package-lock.json`、`npm ci` 未验证、`test/smoke/pack:win` 统一命令未建立** |
| C 共享契约与 Agent 包 | 🟡 源码可编译 | 五个包源码齐全、strict 编译；**缺口：无导出 API 文档、无单元测试（全仓库 0 测试文件）** |
| D 后端 TS 重建 | 🟢 主体完成 | `apps/backend/src` 全套 TypeScript，无 `@ts-nocheck`；本次核验：删除 `dist` 后 `npm run build --workspace @offerget/backend` 从零重建通过；`apps/desktop/dist/main.js` 实际加载 `backend/dist/host.js`（新 TS 产物）。**缺口：迁移时未按计划补行为对照测试与单测** |
| E Desktop/Preload TS 重建 | 🔴 未开始 | `apps/desktop` 仅剩恢复的 `dist` 与声明了 build 脚本的 package.json（无 `tsconfig.json`、无 `src/`，build 必然失败）；`electron/preload.cjs` 仍为恢复 JS |
| F 前端功能回归 | 🟡 构建已解阻 | F-01 已恢复 `src/features/workspace/`，`npm run build` 全量绿；功能回归本身未开始 |
| G 真实 Usage 与联调 | 🔴 未开始 | 依赖 E/F |
| H 测试、发布与验收 | 🔴 未开始 | 无 `tests/` 目录、无测试框架、无 `npm test`；`scripts/` 下 16 个发布/冒烟脚本未挂接 npm 命令 |

## 2. 关键阻断（P0）：根构建失败

本次核验执行 `npm run build`：五个 workspace 包编译通过，**Vite 渲染进程构建失败**（此前记录「build 通过」基于原机器上磁盘残留文件，不代表本检出可构建）。

**根因**：`.gitignore` 中的 `workspace/` 规则无路径前缀，匹配任意层级的同名目录，导致 `src/features/workspace/`（前端工作空间 API 模块，含 `useWorkspaceData`、`workspaceData`、`mutationHelper`、`workspaceActions`）**从未被 Git 跟踪**；本工作区是 git 检出，该目录缺失。`src/app/App.tsx`、`AssistantPage.tsx`、`SettingsPage.tsx` 及 application/resume/settings/conversation/job/profile 各 feature 的 api 层（共 10 个文件）均引用 `../features/workspace/api/*`，Vite 解析失败即整体失败。

**关键事实**：该问题已有修复但滞留未合并——`origin/refactor/frontend-design-v2` 上的提交 `1dc13c5 fix(workspace): 恢复被 gitignore 误忽略的工作空间前端 API`。当前 `main` 与 `feature/backend-ts-rebuild` 均为「构建红」状态。

**影响**：应用不可构建、不可打包、不可冒烟；里程碑 E/F/G/H 全部被前置阻断。

**更新（B-01 执行）**：`feature/backend-ts-rebuild` 已合入 F-01（`d505508`）并复跑 `npm run build`，五个 workspace 包与 Vite 全部构建通过；此 P0 阻断在当前分支已解除。

## 3. 其他发现与风险

1. **dist 生成物入库**：仓库跟踪 142 个 `apps/*/dist`、`packages/*/dist` 文件。基线提交保留恢复产物作为行为基准可以理解（历史中仍可回查），但当前提交继续把新构建产物一并入库，与 `docs/ARCHITECTURE.md`「生成物不作为模块引用源和人工编辑入口」冲突，且会持续制造噪声 diff。
2. **分支管理滞后**：`feature/backend-ts-rebuild` 领先 `main` 1 个提交，未推送未合并；`refactor/frontend-design-v2`（DESIGN v2 前端迁移 + package-lock 生成 + workspace 修复）长期未收敛，三条线并存，修复互相滞留。
3. **后端重建缺测试**：里程碑 D 计划要求「每迁移一个模块做行为对照并补充测试」，实际为一次大提交完成迁移、零测试。旧 JS 基线仍在 git 历史中（b493532），行为对照尚未做，是当前最大的技术债与回归风险源。
4. **产品决策未裁决**：`docs/Agent-design/README.md` 的默认场景「自主岗位搜索」与 PRD「不自动扫描全网岗位、以用户 URL 为主」冲突，README 明确标注「实现前必须更新 PRD，视为待上游确认」。实现前必须先做产品裁决。
5. **可验证性缺口**：Electron 冒烟（Renderer 加载、Backend ready）在 backend 重建后未重新执行；干净安装（`npm ci`）从未验证；Windows 打包门禁在 Linux 工作区不可执行，需保留 Windows 验证路径。

## 4. 下一步计划

### 4.1 任务分配与执行顺序（负责人）

任务详情见三个角色文件：`任务-前端开发.md`、`任务-后端开发.md`、`任务-agent开发.md`。

```text
P0 解阻：F-01 恢复 workspace 前端 API（前端） ──→ B-01 全量构建+Electron 冒烟（后端）
                                                     │
        F-02 design-v2 收敛（前端，P1） ◄───────────┤（分支合并回 main）
                                                     ▼
                              B-02 里程碑 B 收口：lockfile/统一命令/dist 策略（后端）
                                                     ▼
                    B-03 Vitest 基建+后端单测（后端）──→ A-01 agent 五包单测（agent）
                                                     │
      B-04 里程碑 E：Desktop/Preload TS（后端，主干）◄── 并行：F-03 契约消费侧核对（前端）
                                                     ▼
                             F-04 里程碑 F 前端回归（前端）  ←（B-06 ADR 并行，后端）
                                                     ▼
           里程碑 G：A-04 agent 联调 + B-05 数据侧对账 + F-05 UI 展示
并行决策线：A-02 岗位搜索范围（agent 提建议 → PM 裁决 → PRD/设计修订）→ A-03 差距修复
文档收口：F-06 前端专项文档（前端）｜B-06 ADR（后端）
```

| 序号 | 任务 | 负责人 | 依赖 | 对应里程碑 |
| --- | --- | --- | --- | --- |
| 1 | ✅ F-01 恢复 `src/features/workspace/`（构建解阻） | 前端开发 | 无（最先） | B（构建） |
| 2 | ✅ B-01 全量构建验证 + Electron 隔离冒烟 + 契约面抽查 | 后端开发 | F-01 | D 验收 |
| 3 | F-02 design-v2 分支收敛核对与合并 | 前端开发 | F-01 | F（前置） |
| 4 | B-02 lockfile、`npm ci` 验证、统一命令、dist 停止跟踪 | 后端开发 | B-01（F-02 协调） | B |
| 5 | B-03 Vitest 基建 + 后端领域单测（revision/幂等/补偿/脱敏） | 后端开发 | B-02 | C/D/H |
| 6 | A-01 agent 五包单测（不变量可追溯） | agent 开发 | B-03 | C/H |
| 7 | B-04 里程碑 E：Desktop/Preload TS 重建（preload→gateway→adapters→main，每模块一个提交） | 后端开发 | B-01、B-02 | E |
| 8 | F-03 契约消费侧一致性核对（随 B-04 增量） | 前端开发 | F-01、B-04 部分 | E |
| 9 | F-04 里程碑 F 前端功能回归（0.2.0 门禁逐项） | 前端开发 | B-04、F-03 | F |
| 10 | A-02 岗位搜索范围建议 → PM 裁决 → 设计收敛 + ADR | agent 开发 | PM 裁决（部分可先行） | 决策 |
| 11 | A-03 Agent 差距修复 8 项（每项带测试） | agent 开发 | A-01、A-02（1–3 项） | D/F |
| 12 | B-05 里程碑 G 数据侧：usage 入库、Trace 对账 | 后端开发 | B-04、F-04 | G |
| 13 | A-04 里程碑 G agent 侧：Provider usage 归一联调 | agent 开发 | A-01、B-04、A-02 | G |
| 14 | F-05 里程碑 G 前端：真实 Usage 展示 | 前端开发 | F-04、B-05/A-04 | G |
| 15 | B-06 ADR 收口（进程分界/双库/Saga/Bridge） | 后端开发 | 随 B-04 | E/H |
| 16 | F-06 前端交互/响应式/可访问性专项文档 | 前端开发 | F-04 | 文档 |

**关键依赖链**：`F-01 → B-01 → B-02 → B-03 → A-01`；`B-01/B-02 → B-04 → F-04 → 里程碑 G（B-05/A-04/F-05）`。A-02 决策线独立并行，裁决前不阻塞 A-01 与 B-04。

## 5. 本次核验证据

- `apps/backend/dist` 删除后，`npm run build --workspace @offerget/backend` 重建成功（exit 0），产出完整 dist 且与已提交内容一致。
- `npm run build` 全量构建在 Vite 阶段失败，报错指向 `src/app/App.tsx` 的模块解析（`src/features/workspace/api/useWorkspaceData` 不存在）。
- 全仓库（排除 `node_modules`）无任何 `*.test.*` / `*.spec.*` 文件；根与各包 package.json 无 `test` 脚本；根 devDependencies 无测试框架。
- 缺失模块引用面：`grep "features/workspace"` 命中 10 个文件。

**B-01 执行记录（后端开发）**：
- `npm run build` 全量通过（exit 0）：contracts/agent-sdk/agent-core/agent-module-host/agent-modules-defaults/backend 六个 workspace 包 + Vite 渲染进程均构建成功。
- Electron 隔离冒烟通过：`OFFERGET_DESKTOP_SMOKE=1` + 临时 `userData` + `OFFERGET_LIFECYCLE_MODE=recovery`，输出 `rendererLoaded: true`、`backendReady: true`、`startupReadyMs: 538`，并成功经 BackendHost 调用 `workspace:database-recovery-status`（`recoveryMode: healthy`）。
- 冒烟发现并修复 B-01 前隐藏的后端缺陷：`new require('./idempotency-store.js').IdempotencyStore(...)` 在 TS 编译后成为 `new require(...).IdempotencyStore(...)`，实际按 `(new require(...)).IdempotencyStore` 解析导致 Backend 启动崩溃；改为顶部 `const { IdempotencyStore } = require(...)` 后 `new IdempotencyStore(...)` 正常。
- 契约面抽查通过：新 `dist/host.js`、`dist/router.js` 与基线 `b493532` 的导出键、`MethodRoutes`、`ReadOnlyChannels`、`FunctionRouteChannels`、`EventChannels`、`MaxCommandPayloadBytes`、`RestartDelayMs` 完全一致；Router 对成功/失败/未知通道/函数路由/非法 requestId 的结果信封逐项一致。

**R-01 执行记录（后端开发，第三次代码审查通过，待提交合并）**：
- 在 `fix/backend-idempotency-boundary` 分支先补 5 个失败用例（`apps/backend/test/backend-idempotency.test.cjs`），覆盖：业务成功 + 幂等记录写盘失败、同键并发、同键不同 payload、不同内部 requestId 同稳定键重试、Backend 重启重放。
- 实现修复：Router 使用稳定 `idempotencyKey` 作为幂等键（与内部传输 `requestId` 分离），增加同键进程内串行锁与内存回放缓存；`IdempotencyStore.Put` 落盘失败不再向上抛出，业务成功后响应保持成功；Host/Gateway 消息增加 `idempotencyKey` 字段透传。
- 验证：`npm test --workspace @offerget/backend` 5/5 通过；`npm run build` 全量通过；Electron 隔离恢复模式冒烟通过（`backendReady: true`、`recoveryMode: healthy`）。
- PM 首次审查：生产 Gateway 固定向 Host 传 `undefined`，真实写请求绕过回放；整改已补 Contracts/Preload/Gateway 链路。
- PM 第二次审查：**仍请求修改，暂不提交/合并**。根 TypeScript 检查有 30 处错误；附件导入仍发送无键写信封；Mutation 键未在一次意图结束后释放。详见 `docs/rebuild/R-01-CODE-REVIEW-2026-08-20.md` 第 5 节。
- 第二次审查整改（**待第三次审查，尚未提交/合并**）：`useWorkspaceMutation` 显式保留 TanStack 泛型，并在一次 Mutation settle 后释放稳定键；`RemoveConversationMessage` 已恢复完整 payload 转发；所有可重放写信封均强制非空稳定键，附件导入统一经过 `ResolveWriteOptions`。新增缺键拒绝、附件透传与意图键释放用例后，`npm test --workspace @offerget/backend` 8/8 通过，根 `tsc --noEmit -p tsconfig.json` 与 `npm run build` 均通过。
- PM 第三次审查：**通过，可按逻辑拆分提交后合并**。根/Contracts/Backend 类型检查、8 个测试与 Electron 隔离冒烟均通过，第二次审查阻断项全部关闭。

## 6. 待办清单（TODO，负责人见 4.1 与角色任务文件）

- [x] P0 恢复 `src/features/workspace/` 入库并修复 .gitignore（前端 F-01，参考 1dc13c5）
- [x] P0 `npm run build` 全量变绿 + Electron 隔离冒烟复跑（后端 B-01）
- [x] P1 合并 backend TS 与构建修复到 main（`dabb726`）
- [ ] P1 收敛 design-v2 分支（后端/前端 F-02）
- [x] P1 R-01 修复写命令幂等事务边界与稳定幂等键（后端；第三次代码审查通过，待提交合并）
- [ ] P1 生成 package-lock.json，验证干净 `npm ci`（后端 B-02）
- [ ] P1 建立 test/smoke/pack:win 统一命令（后端 B-02）
- [ ] P1 引入测试框架，补后端领域模块单测与契约测试（后端 B-03；agent A-01）
- [ ] P1 决策 dist 跟踪策略（建议停止跟踪，后端 B-02 执行）
- [ ] P2 里程碑 E：Desktop/Preload TS 重建（后端 B-04；前端 F-03 对照）
- [ ] P2 产品裁决：岗位搜索范围（agent A-02 → PM 裁决 → PRD 修订）
- [ ] P2 Agent 差距修复 8 项（agent A-03）
- [ ] P2 里程碑 F/G 回归与联调（前端 F-04/F-05；后端 B-05；agent A-04，按 0.2.0 门禁）
