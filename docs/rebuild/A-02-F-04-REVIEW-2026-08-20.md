# A-02 与 F-04 快速复审报告（2026-08-20）

> 审查原则：灾后重建阶段以时间为重，只检查范围一致性、阻断缺陷和最小回归证据。

## 1. 复审结论

- **A-02：通过并关闭。** 六篇 Agent 设计文档与五份 ADR 已落实 PM 裁决；PRD 与版本路线图原文已经明确 0.2.0 手工录入、0.3.0 用户 URL 提取，因此无需制造重复修改。允许 Agent 开发进入 A-03。
- **F-04 本轮前端交付：通过。** 双分辨率、六个主页面的布局与关键控件检查通过，未发现新的 P0/P1 前端回归。
- **F-04 里程碑：暂不完全关闭。** 真实 Provider、跨会话隔离、键盘/焦点/拖拽以及 Windows 安装升级仍是发布门禁；现有 `output/f04-responsive/visual-result.json` 因 `keyboardNavigation: false` 正确返回 `passed: false`，不得改写成全量通过。
- **代码归属提醒：** 本轮视觉冒烟扩展位于 `apps/desktop/src/main.ts`，按角色边界属于后端/Desktop 所有者。实现本身不因此退回，但提交前须由后端开发认领复核，前端提交不要单独携带 Main 与生成的 `dist/main.js`。

## 2. 最小验证

| 检查 | 结果 |
| --- | --- |
| 根 TypeScript `tsc --noEmit -p tsconfig.json` | 通过 |
| Desktop TypeScript `tsc --noEmit -p apps/desktop/tsconfig.json` | 通过 |
| Vitest | 11 个文件通过；36 passed；6 expected fail |
| `git diff --check` | 通过 |

六条 expected-fail 是 A-03 已登记的实现缺口，不作为 A-02 文档复审或本轮前端回归的新增失败。

## 3. 审查时纠正的文档漂移

`docs/rebuild/任务-agent开发.md` 的 A-03 旧清单仍描述已经转为绿色回归的等待、锁、并发、SSE 与 TurnGroup 项，与 Agent README 第 6 节当前八项缺口不一致。本次由 PM 将任务清单改为以 README 第 6 节为准，避免开发者重复改写已通过路径；同时把 A-04 的 Provider 范围收敛为 0.2.0 仅 DeepSeek，OpenAI 为后续独立 Adapter。

## 4. 下一步安排

1. **立即执行人：Agent 开发。** 开始 A-03 第 1 项：收窄 0.2.0 生产工具清单，并在执行入口按冻结白名单拒绝未授权工具。必须先用现有失败证据复现，修复后把对应 `it.fails` 转为普通回归；一个安全边界一个提交。
2. **前端开发：暂不进入 F-05。** 如具备交互式 Windows/Electron 环境，继续补 F-04 的键盘、焦点与拖拽人工证据；否则等待 B-05/A-04 的 Usage 数据链就绪后再进入 F-05。
3. **后端开发可并行准备 B-05。** 同时认领复核本轮 Desktop 视觉冒烟改动；B-05 只处理 Usage 入库与 Trace 对账，不等待 A-03 全部完成。真实 Provider 联调由 A-04、B-05、F-05 三方统一收口。

## 5. 提交要求

当前工作区包含多个角色的未提交变更。提交前按 A-02 文档、A-01 测试、F-04 冒烟脚本/记录及其他历史变更拆分，不把 `docs/DESIGN.md` 的无关格式调整混入上述提交。
