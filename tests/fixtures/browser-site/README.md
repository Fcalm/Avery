# 浏览器受控测试站

该站点只用于本地、可重复的投递场景验证，覆盖 6 个岗位的搜索、JD、文本字段、普通下拉、所在地与职类两组级联下拉、文件上传、协议确认、一次性提交和稳定回执。

`server.mjs` 只绑定随机 `127.0.0.1` 端口，并提供仅供测试进程断言的 `/__test/state`。由于生产 Runtime 会按设计拒绝 localhost，端到端测试必须通过构造期注入仅匹配本次精确 origin 的导航策略，禁止放宽生产 URL 校验。
